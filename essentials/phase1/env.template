# API Credentials
# No actual API calls in Phase 1, these are placeholders for future integration
OPENAI_API_KEY=your-openai-api-key-here
HUGGINGFACE_API_KEY=your-huggingface-api-key-here

# Environment Settings
ENVIRONMENT=development  # development, testing, production

# Application Settings
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL
DATA_DIR=./data
CACHE_DIR=./cache
TEMP_DIR=./tmp

# Document Processing
MAX_DOCUMENT_SIZE_MB=10
MAX_TOKENS_PER_DOCUMENT=10000
GROBID_HOST=http://localhost
GROBID_PORT=8070
GROBID_TIMEOUT=300
GROBID_THREADS=4

# Vector Database
VECTOR_DB_TYPE=chromadb  # chromadb, faiss, in_memory
VECTOR_DB_DIR=./data/vector_db
VECTOR_DB_COLLECTION=research_papers

# Embedding Model
EMBEDDING_MODEL=all-MiniLM-L6-v2  # SentenceTransformer model name
EMBEDDING_DIMENSION=384
EMBEDDING_BATCH_SIZE=32
EMBEDDING_CACHE_DIR=./data/embeddings

# Scientific APIs
ARXIV_QUERY_LIMIT=100
ARXIV_WAIT_TIME=3  # seconds between requests
SEMANTIC_SCHOLAR_API_KEY=  # Optional, leave empty for public API
SEMANTIC_SCHOLAR_WAIT_TIME=3  # seconds between requests
SEMANTIC_SCHOLAR_CACHE_DIR=./data/semantic_scholar_cache

# LLM Settings
DEFAULT_LLM_MODEL=gpt-3.5-turbo  # For future use
MAX_LLM_TOKENS=4096  # For future use 